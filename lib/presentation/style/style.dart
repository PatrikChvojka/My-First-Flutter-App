import 'package:flutter/material.dart';

class MainAppStyle {
  // font family
  ThemeData themeData = ThemeData(
    fontFamily: 'Raleway',
  );

  // MAIN Color
  Color mainColor = Color.fromRGBO(239, 189, 107, 1);
  Color secondColor = Color.fromRGBO(196, 148, 70, 1);

  // bodyBG
  Color bodyBG = Color.fromRGBO(254, 245, 237, 1);

  // buttonBG
  Color buttonBG = Color.fromRGBO(248, 238, 228, 1);
}
